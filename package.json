{
  "name": "ts_fwd_proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b --pretty",
    "start": "node --enable-source-maps ./dist/index.js",
    "run": "npm run build && npm run start",
    "sv" : "npm run build && npm run start -- --server",
    "cl" : "npm run run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.33"
  }
}
